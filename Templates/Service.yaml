apiVersion: v1
kind: Service
metadata:
  labels:
    app: ms-demo
  name: service2-ms-demo
  namespace: ms-demo
spec:
  selector:
    app: ms-demo
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 4444
      targetPort: 80